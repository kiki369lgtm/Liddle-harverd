# Base URL for the future backend API (Phase Two+).
# Leave unset in Phase One — content services fall back to local data and
# the admissions/contact forms run in demo mode.
# VITE_API_URL=https://api.liddleharverd.ac.ke
